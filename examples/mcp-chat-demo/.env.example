# Copy useful vars to the repository root `.env` (preferred) or set them here.
#
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# MCP_URL=https://tc-helps.mcp.servant.bible/api/mcp
# SERVER_PORT=5275
#
# Optional: default MCP URL in the UI (Vite)
# VITE_MCP_URL=http://localhost:8174/api/mcp
